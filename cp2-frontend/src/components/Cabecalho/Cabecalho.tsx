import Menu from "../Menu/Menu";

export default function Cabecalho() {
  return (
    <header className="flex flex-row items-center justify-center p-8 bg-slate-200">
      <Menu />
    </header>
  );
}
